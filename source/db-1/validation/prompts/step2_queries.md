# Step 2: Queries Extract

**Task**: Extract queries from queries.md into queries.json.

**Reference files**:
- `db-1/queries/queries.md`
- `db-1/queries/queries.json`

**Expected output**: queries.json with exactly 30 queries, each with sql, title, description.

**Pass criteria**: Run `extract_queries_to_json.py`; queries.json has 30 entries.
