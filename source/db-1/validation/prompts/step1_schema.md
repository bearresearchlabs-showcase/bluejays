# Step 1: Schema Parse

**Task**: Validate that the database schema file is syntactically valid SQL.

**Reference files**:
- `db-1/data/schema.sql` or `db-1/data/schema_postgresql.sql`

**Expected output**: Schema file exists and contains at least one `CREATE TABLE` statement.

**Pass criteria**: File parses as valid SQL; no syntax errors.
