---
title: Hierarchical Orders (LinkWay) — Documentation
description: Installation guide, specifications, schema, data dictionary.
database: db-3
---

# Hierarchical Orders (LinkWay) — Documentation

**Database:** db-3  
**Content:** Installation guide, specifications, schema, data dictionary.

---

## Installation Guide

### Step 1: Prerequisites

Ensure PostgreSQL is installed. See specifications for version requirements.

---

### Step 2: Create Database

Create a new database for this schema.

```bash
createdb -U postgres db_3
```

---

### Step 3: Load Schema

Load schema.sql to create tables, indexes, and constraints.

```bash
psql -U postgres -d db_3 -f schema.sql
```

---

### Step 4: Load Data (Optional)

Load production data from data_large.sql when available (>= 1GB). No sample data.

```bash
psql -U postgres -d db_3 -f data_large.sql
```

---

## Specifications

- **PostgreSQL:** 14+
- **Disk:** 100 MB minimum
- **Memory:** 256 MB minimum
- **Platforms:** PostgreSQL

Standard PostgreSQL. No extensions required unless noted.

---

## Schema Overview

**Total tables:** 3

- `table1` — (see data dictionary)
- `table2` — (see data dictionary)
- `table3` — (see data dictionary)

---

## Data Dictionary

### `table1`

- `id` BIGINT PRIMARY KEY
- `parent_id` BIGINT 
- `name` VARCHAR(255) NOT NULL
- `value` NUMERIC(15,2) 
- `category` VARCHAR(100) 
- `date_col` DATE 
- `created_at` TIMESTAMP 
- `updated_at` TIMESTAMP 

### `table2`

- `id` BIGINT PRIMARY KEY
- `table1_id` BIGINT NOT NULL
- `related_value` NUMERIC(15,2) 
- `description` TEXT 
- `date_col` DATE 
- `created_at` TIMESTAMP 

### `table3`

- `id` BIGINT PRIMARY KEY
- `table1_id` BIGINT NOT NULL
- `table2_id` BIGINT NOT NULL
- `metric_value` NUMERIC(15,2) 
- `status` VARCHAR(50) 
- `created_at` TIMESTAMP 

---

*Generated by documentation workflow. MDX-compatible markdown.*
