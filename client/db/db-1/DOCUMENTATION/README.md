---
title: Chat/Messaging System — Documentation
description: Installation guide, specifications, schema, data dictionary.
database: db-1
---

# Chat/Messaging System — Documentation

**Database:** db-1  
**Content:** Installation guide, specifications, schema, data dictionary.

---

## Installation Guide

### Step 1: Prerequisites

Ensure PostgreSQL is installed. See specifications for version requirements.

---

### Step 2: Create Database

Create a new database for this schema.

```bash
createdb -U postgres db_1
```

---

### Step 3: Load Schema

Load schema.sql to create tables, indexes, and constraints.

```bash
psql -U postgres -d db_1 -f schema.sql
```

---

### Step 4: Load Data (Optional)

Load sample data from data.sql if available.

```bash
psql -U postgres -d db_1 -f data.sql
```

---

## Specifications

- **PostgreSQL:** 14+
- **Disk:** 100 MB minimum
- **Memory:** 256 MB minimum
- **Platforms:** PostgreSQL

Standard PostgreSQL. No extensions required unless noted.

---

## Schema Overview

**Total tables:** 12

- `profiles` — (see data dictionary)
- `chats` — (see data dictionary)
- `messages` — (see data dictionary)
- `chat_participants` — (see data dictionary)
- `friends` — (see data dictionary)
- `notifications` — (see data dictionary)
- `file_attachments` — (see data dictionary)
- `anonymous_chats` — (see data dictionary)
- `anonymous_chat_users` — (see data dictionary)
- `anonymous_messages` — (see data dictionary)
- `chat_invitations` — (see data dictionary)
- `aircraft_position_history` — (see data dictionary)

---

## Data Dictionary

### `profiles`

- `id` UUID PRIMARY KEY
- `username` VARCHAR(255) UNIQUE, NOT NULL
- `email` VARCHAR(255) 
- `display_name` VARCHAR(255) 
- `created_at` TIMESTAMP 

### `chats`

- `id` UUID PRIMARY KEY
- `created_by` UUID FOREIGN KEY
- `title` VARCHAR(255) 
- `created_at` TIMESTAMP 

### `messages`

- `id` UUID PRIMARY KEY
- `chat_id` UUID NOT NULL, FOREIGN KEY
- `sender_id` UUID NOT NULL, FOREIGN KEY
- `is_ai` BOOLEAN NOT NULL
- `content` TEXT 
- `created_at` TIMESTAMP 

### `chat_participants`

- `chat_id` UUID NOT NULL, FOREIGN KEY
- `user_id` UUID NOT NULL, FOREIGN KEY
- `joined_at` TIMESTAMP 

### `friends`

- `id` UUID PRIMARY KEY
- `user_id` UUID NOT NULL, FOREIGN KEY
- `friend_id` UUID NOT NULL, FOREIGN KEY
- `status` VARCHAR(50) NOT NULL
- `created_at` TIMESTAMP 

### `notifications`

- `id` UUID PRIMARY KEY
- `user_id` UUID NOT NULL, FOREIGN KEY
- `type` VARCHAR(100) 
- `created_at` TIMESTAMP 
- `read` BOOLEAN 
- `seen_at` TIMESTAMP 

### `file_attachments`

- `id` UUID PRIMARY KEY
- `chat_id` UUID NOT NULL, FOREIGN KEY
- `user_id` UUID NOT NULL, FOREIGN KEY
- `file_name` VARCHAR(255) 
- `file_type` VARCHAR(100) 
- `file_size` BIGINT 
- `created_at` TIMESTAMP 

### `anonymous_chats`

- `id` UUID PRIMARY KEY
- `join_code` VARCHAR(50) 
- `created_at` TIMESTAMP 
- `expires_at` TIMESTAMP 

### `anonymous_chat_users`

- `guest_id` UUID NOT NULL
- `chat_id` UUID NOT NULL, FOREIGN KEY

### `anonymous_messages`

- `id` UUID PRIMARY KEY
- `chat_id` UUID NOT NULL, FOREIGN KEY
- `guest_id` UUID NOT NULL
- `content` TEXT 
- `created_at` TIMESTAMP 

### `chat_invitations`

- `id` UUID PRIMARY KEY
- `inviting_user_id` UUID NOT NULL, FOREIGN KEY
- `invited_user_id` UUID NOT NULL, FOREIGN KEY
- `chat_id` UUID NOT NULL, FOREIGN KEY
- `status` VARCHAR(50) NOT NULL
- `created_at` TIMESTAMP 

### `aircraft_position_history`

- `id` SERIAL PRIMARY KEY
- `hex` VARCHAR(20) NOT NULL
- `speed` NUMERIC(10, 2) 
- `altitude` NUMERIC(10, 2) 
- `timestamp` TIMESTAMP NOT NULL

---

*Generated by documentation workflow. MDX-compatible markdown.*
