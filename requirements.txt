# Root requirements for db repo scripts
# Core (required)
psycopg2-binary>=2.9.0
python-dotenv>=1.2.2
pytest>=8.0.0

# LangGraph (required for gdpval-langgraph harness)
langgraph>=0.2.0

# Anthropic (required for qa_claude, claude_rewrite_evidence)
anthropic>=0.39.0

# JSON Schema (required for validate_queries_json)
jsonschema>=4.20.0

# File-based CDC (watch_source_and_sync)
watchdog>=4.0.0
