# Root requirements for db repo scripts
# Core (required)
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
pytest>=8.0.0

# LangGraph (required for gdpval-langgraph harness)
langgraph>=0.2.0

# Anthropic (required for qa_claude, claude_rewrite_evidence)
anthropic>=0.39.0
